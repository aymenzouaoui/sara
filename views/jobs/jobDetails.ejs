<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Details</title>
</head>
<body>
    <h1>Job Details</h1>
    <h2><%= job.description %></h2>
    <p>Price: <%= job.price %> ETH</p>
    <% if (job.status === 'open') { %>
        <form action="/api/applyForJob" method="post">
            <input type="hidden" name="jobId" value="<%= job.id %>">
            <input type="hidden" name="contractorAddress" value="<%= contractorAddress %>">
            <button type="submit">Apply for Job</button>
        </form>
    <% } else if (job.status === 'pending') { %>
        <p>This job is pending approval.</p>
    <% } else if (job.status === 'completed') { %>
        <p>This job has been completed.</p>
    <% } %>
</body>
</html>
